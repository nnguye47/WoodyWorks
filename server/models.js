const {
  getFirestore,
  collection,
  getDocs,
  doc,
  setDoc,
  deleteDoc,
  updateDoc,
} = require('firebase/firestore');
const { db, catalogCol } = require('../firebase');

module.exports = {
  getFeatured: 
}